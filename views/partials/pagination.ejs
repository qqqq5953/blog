<nav class="my-5" aria-label="Page navigation example">
  <ul class="pagination justify-content-center">
    <li class="page-item <% if(!page.hasPrev){ %> disabled <% } %>">
      <a
        class="page-link"
        href="/?<%= categoryQueryString %>page=<%= page.currentPage - 1%>"
        tabindex="-1"
        >Previous</a
      >
    </li>

    <% for(let i=1; i< page.pageTotal+1; i++){ %>
    <li class="page-item <% if(page.currentPage === i){ %> active <% } %>">
      <a class="page-link" href="/?<%= categoryQueryString %>page=<%- i %>">
        <%= i %>
      </a>
    </li>
    <% } %>

    <li class="page-item <% if(!page.hasNext){ %> disabled <% } %>">
      <a
        class="page-link"
        href="/?<%= categoryQueryString %>page=<%= page.currentPage + 1%>"
        >Next</a
      >
    </li>
  </ul>
</nav>
